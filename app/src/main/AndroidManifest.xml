<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".data.ServixApplication">
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.addEdithabit.AddEditHabitoCategoriaActivity"
            android:label="@string/habit_categoria"/>
        <activity android:name=".ui.dialog.DialogActivity"
            android:theme="@style/Dialog.NoTitle"
            android:excludeFromRecents="true"/>

        <activity android:name=".ui.habitCategoriDetali.ApontEquipamentoDetailActivity"
            android:label="@string/habit_categoria_detalhe"
            />
        <activity android:name=".ui.entidadeHabitoAdd.AddEditHabitoCategoriaActivity"/>
    </application>

</manifest>